.claude/settings.local.json

# Secrets — never commit. setup/.env.example is the safe template that IS committed.
.env
.env.local
.env.*.local
*.pem
setup/secrets/

# Node / build outputs (npm workspaces)
node_modules/
dist/
.next/
*.tsbuildinfo
