{
  "name": "@gitcade-examples/pong",
  "private": true,
  "version": "0.1.0",
  "description": "Pong — the Phase 1 proof game, built entirely from SDK primitives + JSON (zero custom code).",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3003",
    "build": "vite build",
    "test": "vitest run",
    "validate": "gitcade validate ."
  },
  "dependencies": {
    "@gitcade/sdk": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
