{
  "name": "gitcade-survival-arena",
  "private": true,
  "version": "1.0.0",
  "description": "survival-arena — a GitCade ecosystem seed game composed from @gitcade/library parts.",
  "type": "module",
  "scripts": {
    "sync-assets": "node scripts/sync-assets.mjs",
    "predev": "npm run sync-assets",
    "prebuild": "npm run sync-assets",
    "pretest": "npm run sync-assets",
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "validate": "gitcade validate ."
  },
  "dependencies": {
    "@gitcade/sdk": "1.9.0",
    "@gitcade/library": "1.9.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
