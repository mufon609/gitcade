{
  "name": "@gitcade-proofs/arena-reskin",
  "private": true,
  "version": "0.1.0",
  "description": "Presentational proof — the survival-arena logic demo re-skinned with generated sprites, a starfield + space tileset, synthesized SFX + chiptune music, and explosion/sparkle particles.",
  "type": "module",
  "scripts": {
    "sync-assets": "rm -rf public/assets && cp -r ../../assets public/assets",
    "predev": "npm run sync-assets",
    "prebuild": "npm run sync-assets",
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "validate": "gitcade validate ."
  },
  "dependencies": {
    "@gitcade/sdk": "*",
    "@gitcade/library": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
