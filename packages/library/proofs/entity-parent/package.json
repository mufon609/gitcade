{
  "name": "@gitcade-proofs/entity-parent",
  "private": true,
  "version": "0.1.0",
  "description": "Reuse proof — the 0.9.0 entity HIERARCHY / transform parenting (scene graph): tween-driven parents carry statically-parented children (translation, multi-level chain, rotation orbit), plus runtime attachTo/detach pickup.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "validate": "gitcade validate ."
  },
  "dependencies": {
    "@gitcade/sdk": "*",
    "@gitcade/library": "*"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
