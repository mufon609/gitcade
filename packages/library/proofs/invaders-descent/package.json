{
  "name": "@gitcade-proofs/invaders-descent",
  "private": true,
  "version": "0.1.0",
  "description": "Reuse proof #4 — space-invaders descent. The same four parts: invaders spawn in waves, ai-chase the player with a Y-axis lock (straight descent), contact-damage on contact, and die (health-and-death) to the ship's bullets.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "validate": "gitcade validate ."
  },
  "dependencies": {
    "@gitcade/sdk": "*",
    "@gitcade/library": "*"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
