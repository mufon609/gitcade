{
  "name": "@gitcade-proofs/platformer-push",
  "private": true,
  "version": "0.1.0",
  "description": "Reuse proof — two-body PUSH (the resolveBodies push step): a player shoves pushable crates sideways, into a wall (pusher stops behind), off a ledge (the crate falls), and a 2-crate chain.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "validate": "gitcade validate ."
  },
  "dependencies": {
    "@gitcade/sdk": "1.4.0",
    "@gitcade/library": "1.4.0"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
