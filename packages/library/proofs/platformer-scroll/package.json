{
  "name": "@gitcade-proofs/platformer-scroll",
  "private": true,
  "version": "0.1.0",
  "description": "Reuse proof — a scrolling tile platformer exercising the 0.7.0 camera + tilemap collision enablers (camera-follow, tilemap-collide, move-platformer).",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "validate": "gitcade validate ."
  },
  "dependencies": {
    "@gitcade/sdk": "*",
    "@gitcade/library": "*"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
