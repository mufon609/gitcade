{
  "name": "@gitcade-proofs/platformer-slopes",
  "private": true,
  "version": "0.1.0",
  "description": "Reuse proof — the 0.11.0 platformer terrain: floor SLOPES (resolveSlopes via tilemap-collide) the player walks up/down, plus a LADDER the player climbs (move-platformer climb mode).",
  "type": "module",
  "scripts": { "test": "vitest run", "validate": "gitcade validate ." },
  "dependencies": { "@gitcade/sdk": "0.11.0", "@gitcade/library": "0.11.0" },
  "devDependencies": { "vitest": "^2.1.0" }
}
