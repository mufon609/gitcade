{
  "name": "@gitcade-proofs/platformer-solids",
  "private": true,
  "version": "0.1.0",
  "description": "Reuse proof — entity-vs-entity solid resolution + swept collision (solid-collide, resolveSolids sweep).",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "validate": "gitcade validate ."
  },
  "dependencies": {
    "@gitcade/sdk": "*",
    "@gitcade/library": "*"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
