{
  "name": "@gitcade-proofs/platformer-stack",
  "private": true,
  "version": "0.1.0",
  "description": "Reuse proof — dynamic-on-dynamic stacking (the resolveBodies stacking step): a pushable crate is solid-to-dynamics, so a player stands on a crate, crates stack, and a rider rides a pushed/carried crate.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "validate": "gitcade validate ."
  },
  "dependencies": {
    "@gitcade/sdk": "*",
    "@gitcade/library": "*"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
