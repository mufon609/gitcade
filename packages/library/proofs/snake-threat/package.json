{
  "name": "@gitcade-proofs/snake-threat",
  "private": true,
  "version": "0.1.0",
  "description": "Reuse proof #1 — snake-tail threat. The four reuse parts (ai-chase + contact-damage + wave-spawner + health-and-death) as a swarm of chasers hunting the player.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "validate": "gitcade validate ."
  },
  "dependencies": {
    "@gitcade/sdk": "0.1.0",
    "@gitcade/library": "0.1.0"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
