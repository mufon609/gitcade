{
  "name": "@gitcade/artifact-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GitCade artifact server — streams built game artifacts from S3/MinIO with the strict game CSP, correct content-types, and immutable cache headers. The ONLY way games are served (no presigned URLs, no raw bucket exposure).",
  "bin": {
    "gitcade-artifact-server": "bin/server.mjs"
  },
  "scripts": {
    "start": "tsx src/server.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
