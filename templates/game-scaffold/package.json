{
  "name": "@gitcade-template/game-scaffold",
  "private": true,
  "version": "0.1.0",
  "description": "GitCade ecosystem game scaffold — the compliant starting point for a new game.",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3002",
    "build": "vite build",
    "test": "vitest run",
    "validate": "gitcade validate ."
  },
  "dependencies": {
    "@gitcade/sdk": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
